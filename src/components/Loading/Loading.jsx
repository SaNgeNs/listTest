import React from 'react';

import './Loading.less';

export const Loading = () => (
  <p className="Loading">
    <span className="Loading__line" />
    <span className="Loading__line" />
    <span className="Loading__line" />
  </p>
);

export default Loading;
